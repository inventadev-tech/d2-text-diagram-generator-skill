[project]
name = "skill-flow-diagram"
version = "0.1.0"
description = "Codex skill for rendering D2 diagrams as Unicode text."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
